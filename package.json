{
  "name": "changescope",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "start": "vite --host 0.0.0.0",
    "build": "rm -rf dist && mkdir dist && cp index.html dist/index.html && cp -R src dist/src",
    "preview": "vite preview --host 0.0.0.0"
  },
  "devDependencies": {}
}
